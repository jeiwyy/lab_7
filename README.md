# Сурков Яков КМБ-1 Лабароторная №7

# Задание 1. Текстовые файлы
## Задача 4
### Текст задачи
Для заданного файла возвратить true, если он не содержит нуля, и false в противном случае.

### Описание логики программы
Программа начинается с ввода пользователем названия файла. После чего создается объект с этим файлом, также проверяется корректность имени файла, и его существовние. Если название некорректно, пользватель должен ввести название по новой. Если файл не существует, то создается пустой файл.
Далее генерируется несколько случайных целых чисел, которыми перезаписывается содержание файла.
После содержимое файла считывается и значение каждой строки сравнивается с нулем, если ноль есть в файле возвращается истиное значение, в ином случае ложное значение.
### Тестирование
![](pics/task1.png)

# Задание 2. Текстовые файлы
## Задача 4
### Текст задачи
Вычислить максимальный элемент.
### Описание логики программы
Программа начинается с ввода пользователем названия файла. После чего создается объект с этим файлом, также проверяется корректность имени файла, и его существовние. Если название некорректно, пользватель должен ввести название по новой. Если файл не существует, то создается пустой файл.
Далее генерируется несколько случайных целых чисел, которыми перезаписывается содержание файла.
После содержимое файла считывается и все значения поэтапно сравниваются с минимальным значением, если текущее значение больше, то минимальное изменяется на текущее значение.
По итогу выводится наибольшее значение из файла.

### Тестирование
![](pics/task2.png)

# Задание 3. Текстовые файлы
## Задача 4
### Текст задачи
Переписать в другой файл строки, оканчивающиеся на заданный символ.
### Описание логики программы
Программа начинается с вопроса пользователю создавать ли случайный файл:
 - Если да, то создается файл с случайными строковыми значениями
 - Если нет, то пользователь вводит название файла, также проверяется корректность названия и его существование аналогично с прошлыми задачами.

Далее программа запрашивает у пользователя символ с которым будут сравниваться строки файла.
После программа сравнивает окончание строки файла с символом пользователя, если строка заканичвается на этот символ - программа добавляет эту строку в результат.
Далее получившаяся строка записывается в новый файл, который укажет пользователь, и этот файл выводится
### Тестирование
![](pics/task3.png)

# Задание 4. Бинарные файлы
## Задача 4
### Текст задачи
Подсчитать количество пар противоположных чисел среди компонент исходного файла.
### Описание логики программы
Программа запрашивает у пользователя количество элементов и название бинарного файла. Если файл с таким именем не существует, он создается (с расширением `.bin`). Затем файл заполняется заданным количеством случайных целых чисел в диапазоне от -20 до 20. 
После этого программа считывает все числа из бинарного файла в массив. Далее массив перебирается, и для каждого элемента проверяется, содержит ли он противоположное ему число (число с обратным знаком). Поскольку при таком алгоритме каждая пара находится дважды, итоговое количество найденных совпадений делится на 2 и выводится на экран.

### Тестирование
![](pics/task4.png)

# Задание 5. Бинарные файлы и структуры
## Задача 4
### Текст задачи
Информация о багаже пассажира описывается массивом, где каждый элемент содержит название единицы багажа (чемодан, сумка, коробка и т.д.) и ее массу. Дан файл, содержащий сведения о багаже нескольких пассажиров. На сколько багаж максимальной массы отличается от багажа минимальной массы?
### Описание логики программы
Программа запрашивает у пользователя количество пассажиров и название файла. Затем генерируется  массив массивов, где каждому пассажиру назначается от 1 до 4 предметов багажа со случайным названием и случайным весом. Сгенерированные данные сохраняются в бинарный файл при помощи XML-сериализации. 
Затем происходит обратное чтение данных из файла. Программа проходит циклом по всем предметам багажа каждого из пассажиров и последовательно сравнивает их вес для поиска абсолютно максимального и минимального значения массы. В результате вычисляется и выводится на экран разница между массой самого тяжелого и самого легкого предмета багажа.

### Тестирование
![](pics/task5.png)

# Задание 6. List
## Задача 4
### Текст задачи
Составить программу, которая удаляет из списка L за каждым вхождением элемента E один элемент, если такой есть, и он отличен от E.
### Описание логики программы
Программа запрашивает количество элементов, после чего генерирует список из случайных строковых значений длиной в 3 символа и выводит его. Затем пользователь вводит значение элемента `E`. 
Создается новый пустой список-результат, куда сразу добавляется первый элемент исходного списка. Далее программа перебирает остальные элементы: если предыдущий элемент не был равен заданному значению `E`, то текущий элемент добавляется в новый список. Таким образом, из списка исключаются элементы, идущие сразу за найденным элементом `E`. Итоговый список выводится на экран.


### Тестирование
![](pics/task6.png)

# Задание 7. LinkedList
## Задача 4
### Текст задачи
Определить, есть ли в списке L хотя бы один элемент, который равен следующему за ним (по кругу) элементу (первый элемент считать следующим для последнего).
### Описание логики программы
Программа запрашивает количество элементов и генерирует связный список из случайных символов, после чего выводит его. 
Для проверки условия программа перебирает элементы списка: значение каждого текущего узла сравнивается со значением следующего за ним. Если достигается конец списка (следующий элемент равен `null`), то значение последнего элемента сравнивается с самым первым узлом списка. Если находится хотя бы одна пара одинаковых соседних элементов, функция возвращает `true`, иначе - `false`. На основе результата выводится соответствующее текстовое сообщение.

### Тестирование
![](pics/task7.png)

# Задание 8. HashSet
## Задача 4
### Текст задачи
Есть перечень стран, популярных у туристов. Определить для каждой страны, какие из них посетили все n туристов, какие — некоторые из туристов, и какие — никто из туристов.
### Описание логики программы
В программе инициализированы исходные данные: множество, содержащее перечень всех популярных стран, и список множеств туристов, где в каждом множестве лежат страны, которые посетил конкретный турист.
С помощью встроенных методов работы с множествами программа вычисляет:
- Страны, которые посетили все туристы (используется операция пересечения `IntersectWith` для всех множеств туристов).
- Страны, которые посетил хотя бы один турист (используется операция объединения `UnionWith`).
- Страны, которые никто не посетил (из множества всех стран исключаются все посещенные страны с помощью операции разности `ExceptWith`).
Итоговые списки стран объединяются в строку и выводятся.

### Тестирование
![](pics/task8.png)

# Задание 9. HashSet
## Задача 4
### Текст задачи
Файл содержит текст на русском языке. Напечатать в алфавитном порядке все глухие согласные буквы, которые не входят ровно в одно слово.
### Описание логики программы
Программа инициализирует множество со списком всех глухих согласных букв. Затем запрашивается название текстового файла, программа считывает его содержимое, переводит в нижний регистр и разбивает на отдельные слова. 
Для каждого слова определяются уникальные символы, и среди них отбираются только глухие согласные. Используя дополнительные множества, программа отслеживает буквы, встретившиеся впервые, и буквы, встретившиеся более чем в одном слове. На основе этого вычисляется множество согласных, которые встретились ровно в одном слове. В конце эти буквы исключаются из изначального списка глухих согласных, и результат (буквы, которые ни разу не встретились или встретились несколько раз) выводится.

### Тестирование
![](pics/task9.png)

# Задание 10. Dictionary/SortedList
## Задача 4
### Текст задачи
В некотором вузе абитуриенты проходят предварительное тестирование, по результатам которого могут быть допущены к сдаче вступительных экзаменов в первом потоке.
Тестирование проводится по двум предметам, по каждому предмету абитуриент может набрать от 0 до 100 баллов. При этом к сдаче экзаменов в первом потоке допускаются абитуриенты, набравшие по результатам тестирования не менее `30` баллов по каждому из двух предметов. На вход программы подаются сведения о результатах предварительного тестирования. Известно, что общее количество участников тестирования не превосходит 500.

В первой строке вводится количество абитуриентов, принимавших участие в тестировании, N. Далее следуют N строк, имеющих следующий формат:

`<Фамилия><Имя><Баллы>`

Здесь `<Фамилия>` – строка, состоящая не более чем из 20 символов;

`<Имя>` – строка, состоящая не более чем из 15 символов;

`<Баллы>` – строка, содержащая два целых числа, разделенных пробелом, соответствующих баллам, полученным на тестировании по каждому из двух предметов. 

При этом `<Фамилия>` и `<Имя>`, `<Имя>` и `<Баллы>` разделены одним пробелом.

Примеры входных строк:

`Ветров Роман 68 59`

`Анисимова Екатерина 64 88`

Напишите программу, которая будет выводить на экран фамилии и имена абитуриентов, потерпевших неудачу, то есть не допущенных к сдаче экзаменов в первом потоке. При этом фамилии должны выводиться в алфавитном порядке.

### Описание логики программы
Программа запрашивает текстовый файл с результатами тестирования. Файл считывается построчно, каждая строка разделяется по пробелам на составляющие (фамилия, имя, баллы по двум предметам). Баллы проходят валидацию (оба числа должны быть от 0 до 100).
Корректные данные добавляются в отсортированный список, где ключом является полное имя абитуриента, а значением - целочисленный массив из двух его баллов. Использование такой структуры гарантирует автоматическую сортировку всех записей по ключу (то есть по алфавиту).
После заполнения списка программа перебирает всех абитуриентов, сравнивает их баллы и выводит на экран имена тех, чьи результаты удовлетворяют условиям. Затем осуществляется вывод.

### Тестирование
![](pics/task10.png)
